{
  "name": "botica-nova-salud",
  "version": "1.0.0",
  "description": "Sistema de gestión de inventario, ventas y atención al cliente para Botica Nova Salud",
  "private": true,
  "license": "ISC",
  "type": "commonjs",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
